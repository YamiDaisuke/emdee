import Core

// WebRenderer module — web server, JSON API, and embedded browser assets.
public struct WebRenderer {
    private let logger: Logger

    public init(logger: Logger) {
        self.logger = logger
    }
}
