opt_in_rules:
  - force_cast
  - trailing_whitespace

line_length:
  warning: 120
  error: 160

trailing_whitespace:
  ignores_empty_lines: true

force_cast: error

excluded:
  - .build
  - Package.swift
