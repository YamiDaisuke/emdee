opt_in_rules:
  - force_unwrapping
  - empty_count
  - closure_spacing

line_length:
  warning: 120
  error: 160

trailing_whitespace:
  ignores_empty_lines: true

force_cast: error

excluded:
  - .build
  - Package.swift
